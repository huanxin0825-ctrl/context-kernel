# v0.1.25

This release reduces false loop-guard failures during multi-step agent work.

## Fixed

- The agent loop guard now stops only after repeated identical actions, not after the first duplicate.
- This allows normal retry, verification, and recovery flows such as repeated safe commands or file reads.
- Loop-guard diagnostics now describe the issue as repeated actions instead of a single duplicate.
