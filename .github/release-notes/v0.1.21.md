# v0.1.21

This release improves command failure visibility in the interactive chat experience.

## Changed

- `Run failed: command_failed` messages now include a `Reason:` line.
- Failed `run_command` summaries include the command string, exit code, and stdout/stderr snippets when available.
